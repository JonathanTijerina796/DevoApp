rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Reglas permisivas temporalmente - validación de seguridad en código
    // TODO: Ajustar reglas más restrictivas cuando se resuelva el problema
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
